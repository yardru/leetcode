#include <iostream>
#include <vector>

#include "N-Queens.h"

using namespace std;

class Solution {
public:
    void run(void) {
        struct {
        } tests[] = {
            {}
        };
        for (auto & test : tests)
            std::cout << "!!!\n";
    }
};

int main(void) {
    Solution51().run();
    return 0;
}
