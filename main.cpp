#include <iostream>
#include <vector>

#include "Minimum Reverse Operations.h"

using namespace std;

class Solution {
public:
    void run(void) {
        struct {
        } tests[] = {
            {}
        };
        for (auto & test : tests)
            std::cout << "!!!\n";
    }
};

int main(void) {
    Solution2612().run();
    return 0;
}
